import { ReactElement } from "react";
declare const CustomIframe: ({ children, title, id, }: {
    children: ReactElement;
    title: string;
    id: string;
}) => JSX.Element;
export default CustomIframe;
